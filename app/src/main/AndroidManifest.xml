<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fete">
    <application
        android:allowBackup="true"
        android:icon="@drawable/fete1"
        android:label="@string/app_name"
        android:roundIcon="@drawable/fete1"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.MapsActivity_Evento"></activity>
        <activity android:name=".Activities.EventoVisionGeneral" />
        <activity android:name=".Activities.EventosActivity" />
        <activity android:name=".Activities.ReseniaForm" />
        <activity
            android:name=".Activities.MainActivity"
            android:label="@string/title_activity_main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />

        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="@string/google_maps_key" />
    </application>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
</manifest>